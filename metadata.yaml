name: gitlab-runner
summary: Gitlab-runner is a charm to install and configure a gitlab runner
maintainer: Chris MacNaughton <gitlab-runner@chris.centauri.solutions>
description: |
  Gitlab-runner is a charm to install and configure a gitlab runner
tags:
  # Replace "misc" with one or more whitelisted tags from this list:
  # https://jujucharms.com/docs/stable/authors-charm-metadata
  - misc
subordinate: false
series:
  - bionic
# provides:
#   provides-relation:
#     interface: interface-name
# requires:
#   requires-relation:
#     interface: interface-name
# peers:
#   peer-relation:
#     interface: interface-name
